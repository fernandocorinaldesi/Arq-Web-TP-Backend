/*const express = require("express");
const router = express.Router();
const tokenController = require("../../controllers/core/token");


router.post("/refreshToken",tokenController.getRefresh);
router.post("/rejectToken",tokenController.rejectToken);

module.exports = router;*/